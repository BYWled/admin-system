# React 后台管理系统

>基于React 19的后台管理系统demo

[![GitHub stars](https://img.shields.io/github/stars/BYWled/admin-system?style=flat-square&logo=github)](https://github.com/BYWled/admin-system) [![Gitee](https://img.shields.io/badge/Gitee-Project-c71d23?style=flat-square&logo=gitee&logoColor=white)](https://gitee.com/BYWled/admin-system)

## 项目简介

本项目是一个完整的React后台管理系统框架，包含用户认证、权限管理等核心功能模块。项目使用现代化的前端开发技术栈，提供开箱即用的管理后台解决方案。

---

## ⚠️ 重要声明

### HTTP 协议要求

**本项目需要使用 HTTP 协议发起连接，暂不支持 HTTPS。**

后端连接地址配置在 `src/utils/service.js` 中的 `baseURL` 字段：
```javascript
baseURL: 'http://***.***.***.***'
```

确保后端服务使用 HTTP 协议运行，否则会产生连接错误。

### 实验性功能声明

- **Temporal API**：项目使用了 JavaScript 最新的实验性日期/时间 API `Temporal`（如 `Temporal.Now.zonedDateTimeISO()` 等），用于更精确的时间处理。由于该 API 目前处于提案的实验性阶段，建议在支持该特性的环境或配合 Polyfill 使用。
- **React 19 新特性**：项目基于 React 19.2.0，使用了最新的 React API。
- **React Router DOM 7**: 使用动态路由和新版本的路由 API。

---

## 🔑 核心功能

### 1. 用户认证与授权
- 基于 Token 的身份验证
- 权限守卫（BeforeEach）
- localStorage 本地存储管理

### 2. 权限管理
- 动态路由配置
- 权限校验机制
- 角色权限分配

---

## 📋 技术栈

| 类别       | 技术             | 版本    |
| ---------- | ---------------- | ------- |
| 框架       | React            | ^19.2.0 |
| 路由       | React Router DOM | ^7.12.0 |
| UI组件库   | Ant Design       | ^6.1.4  |
| HTTP客户端 | Axios            | ^1.13.2 |
| 构建工具   | Vite             | ^7.2.4  |
| 样式处理   | Sass/SCSS        | ^1.97.2 |
| 加密工具   | js-md5           | ^0.8.3  |

---

## 📁 项目结构

```
src/
├── main.jsx                 # 应用入口
├── api/                     # API 接口模块
│   ├── loginApi.js         # 登录相关接口
│   └── ......              # 其它业务相关接口
├── router/
│   └── index.jsx           # 路由配置与权限守卫
├── utils/
│   ├── service.js          # Axios 实例配置（HTTP 服务）
│   └── verifyCode.jsx      # 验证码组件
├── views/                  # 页面组件
│   ├── login/             # 登录页面
│   ├── layout/            # 布局容器
│   └── ......             # 其它组件
├── assets/                # 静态资源
└── styles/               # 全局样式

```

---

## 🚀 快速开始

### 环境要求

- Node.js >= 14
- npm >= 6 或 yarn >= 1.22

### 安装依赖

```bash
npm install
```

### 开发运行

```bash
npm run dev
```

启动后访问 `http://localhost:5173` (Vite 默认端口)

### 构建部署

```bash
npm run build
```

### 代码检查

```bash
npm run lint
```

---

**请注意**：本项目在开发期间请访问[dev分支](https://gitee.com/BYWled/admin-system/commits/dev)查看代码详情，主分支仅保留React默认代码
